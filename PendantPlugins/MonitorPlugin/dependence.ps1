param($type,$sourceDir)
$pwdPath = Get-Location
echo "==============make monitor plugin dependence=============="
echo $type
echo $sourceDir
echo "==========================finished==========================="
